# Copy to .env and fill in. SEC requires a declared User-Agent with contact info.
SEC_USER_AGENT="equity-filing-delta pranitkatwe157@gmail.com"

# Optional price source (free tier). Stooq needs no key; Tiingo does.
TIINGO_API_KEY=""

# LLM extraction (Step 3+). Local/gated — runs on your key.
ANTHROPIC_API_KEY=""
