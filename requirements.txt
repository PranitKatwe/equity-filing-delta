# Dependencies for the Vercel serverless function only (api/narrate.py).
# The narrator uses GLM 5.2 via NVIDIA's OpenAI-compatible endpoint, so the
# OpenAI SDK is the only runtime dep — deliberately lightweight (no pandas)
# for fast cold starts. The pipeline's full deps live in pyproject.toml.
openai>=1.30
